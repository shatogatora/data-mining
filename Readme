docker-compose up -d && docker-compose logs -f

実際にコンテナ立ち上げまではできたのですが、jupyter lab上で実行しようとしたところ、コンフリクトだらけでした。
なのでDockerfileでrequirements.txtからpip installしようとしている部分はコメントアウトしてあります
そのため、requirements.txtについては結局使ってないので、無視していただけると幸いです。
